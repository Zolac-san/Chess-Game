#ifndef MINMAXSTATE
#define MINMAXSTATE


/**
 * \enum MinMaxState
 * \brief Enumeration des etats du min-max
 * 
 * Enumeration qui liste les differrents etats du min-max soit min et max
 * 
 */
enum MinMaxState{
    MIN,
    MAX
};


#endif
